{
  "name": "vite-ssr-bundle-issue",
  "version": "1.0.0",
  "devDependencies": {
    "vite": "^3.0.0-beta.10"
  },
  "private": true,
  "scripts": {
    "build": "vite build --ssr src/index.js --outDir dist/server",
    "dev": "node --enable-source-maps index.js",
    "dev:inspect": "node --inspect --enable-source-maps index.js",
    "start": "node --enable-source-maps index.js",
    "postinstall": "cp -r ./test-package ./node_modules/test-package"
  },
  "type": "module"
}
